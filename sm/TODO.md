# Settings
[X] @sm ... (everything that starts with @ is a settings and instructions must be unlocked by enabling) 8/16/32/64 bit size
[ ] @input (ascii/int) allows which to go + enables inputting
[ ] @output (ascii/int) allows which to cout + enables printing
[ ] @operators allows operators :)

# Esolang
[X] Stack mem
[X] Memory mem

[X] push (unlocked through @sm)
[X] pop (unlocked through @sm)
[X] learn (unlocked through @sm), first pop the value, second pop the memory index
[X] remember (unlocked through @sm)
[ ] receive (unlocked through @input), takes in the input as an ascii character if ascii and an integer if int, then push to the top of the stack
[ ] log (unlocked through @output), prints the top of the stack as an ascii character if ascii and an integer if int
[ ] add (unlocked through @operators)
[ ] sub (unlocked through @operators)
[ ] mult (unlocked through @operators)
[ ] div (unlocked through @operators), int division